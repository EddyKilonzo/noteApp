<%- include('partials/header') %>


<main class="container">
    <ul id="notes">
        <% for(let note of notes) { %>
            <li class="note"><a href=" /note/<%= note.id %> "><%= `${note.title}` %></a></li>
        <% } %>
    </ul>
</main>


<%- include('partials/footer') %> 
