<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/css/style.css">
    <title>NotesApp</title>
</head>
<body>
    <header class="container">
        <h1 id="logo">NotesApp</h1>
        <nav>
            <ul>
                <li class="nav-links"><a href="/">Home</a></li>

                <% if(locals.isLoggedIn) { %>
                    <li class="nav-links"><a href="/notes">Notes</a></li>
                    <li class="nav-links"><a href="/create">New Note</a></li>
                    <li class="nav-links"><a href="/logout">Log Out</a></li>
                <% } else { %>  
                    <li class="nav-links"><a href="/login">Log In</a></li>
                    <li class="nav-links"><a href="/signup">Sign Up</a></li>
               <% } %>
            </ul>
        </nav>
    </header>
    <% if(locals.isLoggedIn) { %>
        <p class="container" style="margin: 10px 0;">Welcome, <%= locals.username %></p>
    <% } %>